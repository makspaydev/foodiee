{
  "name": "foodiee-backend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Foodiee ordering backend — OAuth 2.1 + PKCE against Swiggy MCP, calls the Instamart MCP on the user's behalf with explicit confirmation.",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "register": "node src/register-client.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "cookie-parser": "^1.4.7",
    "express": "^4.21.2"
  }
}
